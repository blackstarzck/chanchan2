# Changesets

Use Changesets to record publishable package changes:

```bash
npm run changeset
```

Version packages locally:

```bash
npm run version-packages
```

Publish from CI:

```bash
npm run release
```

